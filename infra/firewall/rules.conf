# Inbound allowlist applied inside the app's network namespace (iptables sidecar).
# Format: <proto> <port> <comment>. Everything else inbound is DROPPED.
tcp 8080 topics-api http + actuator (/manage)
