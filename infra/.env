# Compose project defaults. RUNTIME_IMAGE must match infra/terraform/variables.tf (runtime_image).
COMPOSE_PROJECT_NAME=topics
RUNTIME_IMAGE=eclipse-temurin:11-jre
APP_VERSION=0.1.0
DD_ENV=legacy
PROMETHEUS_PORT=9091
GRAFANA_PORT=3000
